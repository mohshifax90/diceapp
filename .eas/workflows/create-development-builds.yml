name: Create Development Builds

on:
  push:
    branches:
      - main
      - master

jobs:
  build_android:
    name: Build Android Development Client
    type: build
    params:
      platform: android
      profile: development
